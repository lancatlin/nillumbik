import { Container } from '@mantine/core'
import React, { type JSX } from 'react'

const Home: React.FC = (): JSX.Element => {
    return (
        <section>
            <Container>
                Home Page
            </Container>
        </section>
    )
}

export default Home